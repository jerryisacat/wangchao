{
  "name": "@wangchao/sources",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node -e \"import('./dist/discovery.fixtures.js').then((m) => m.runSourceDiscoveryFixtures()).then(() => import('./dist/parser.fixtures.js')).then((m) => m.runSourceParserFixtures()).then(() => import('./dist/adapters.fixtures.js')).then((m) => m.runAdaptersFixtures())\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "fast-xml-parser": "^5.10.0",
    "linkedom": "^0.18.13"
  }
}
